# 여유로 서울 (Yeoyuro Seoul) — 앱 배포용 런타임 의존성
# Streamlit Community Cloud 는 루트 requirements.txt 만 자동 인식하므로
# 이 파일에는 앱 실행에 실제로 import 되는 패키지만 둔다.
# 파이프라인 재현(스크립트 실행·모델 학습·검증)은 requirements-dev.txt 를 쓴다.

streamlit>=1.36
pandas>=2.2
numpy>=1.26
plotly>=5.24          # 클릭형 벡터 노선도. minallowed/maxallowed 는 5.24+ 필요
pyarrow>=15.0         # parquet 마트 읽기
tzdata                # Asia/Seoul 기본 시각. 컨테이너에 tz 데이터가 없는 경우 대비
