# MetroCalm 실행 의존성
# Python 3.11 이상. 3.14 에서도 동작을 확인했다.
#   pip install -r requirements.txt

# --- 데이터 처리 ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet. 없으면 스크립트가 csv.gz 로 폴백한다
openpyxl>=3.1          # 원본 xlsx / 좌표 워크북 읽기

# --- 모델링 ---
scikit-learn>=1.4      # PR-AUC, precision-recall curve
lightgbm>=4.3
shap>=0.45             # Model-2 설명가능성

# --- 그래프 ---
networkx>=3.2

# --- 검증 ---
pandera>=0.20          # 스키마 검증
pytest>=8.0

# --- 시각화 ---
matplotlib>=3.8
plotly>=5.24           # 클릭형 벡터 노선도. minallowed/maxallowed 는 5.24+ 필요

# --- 서비스 ---
streamlit>=1.36
